<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zwl.mall.dao.mapper.UserAccountMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.zwl.mall.dao.model.UserAccount">
        <id column="id" property="id"/>
        <result column="uid" property="uid"/>
        <result column="out_open_id" property="outOpenId"/>
        <result column="out_trade_no" property="outTradeNo"/>
        <result column="money" property="money"/>
        <result column="type" property="type"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, uid,out_open_id,out_trade_no, money, `type`, create_time, update_time, deleted
    </sql>

    <select id="getBTCInfoByUid" resultType="decimal">
        SELECT
            ifnull(sum( money * `type` ),0)
        FROM
            user_account
        WHERE
            uid = #{uid}
    </select>
    <select id="isTodayComplete" resultType="int">
SELECT
	count( * )

	FROM
		user_account
	WHERE
		uid = #{uid}
		AND deleted = 0
AND DATE_FORMAT( create_time, '%y%m%d' ) = DATE_FORMAT(  now( ), '%y%m%d' )

    </select>


</mapper>
