<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zwl.mall.dao.mapper.UserCalculationPowerMapper">
    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.zwl.mall.dao.model.UserCalculationPower">
        <id column="id" property="id"/>
        <result column="uid" property="uid"/>
        <result column="nickname" property="nickname"/>
        <result column="power_value" property="powerValue"/>
        <result column="type" property="type"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, uid,nickname, power_value, type, create_time, update_time, deleted
    </sql>
    <update id="resetByUid">
        update user_calculation_power set deleted=1 where  where uid =#{uid} and deleted=0 and type =1
    </update>
    <select id="getAblePowerByUid" resultType="java.lang.Integer">
        select  500+ifnull(sum(power_value),0) from user_calculation_power where uid =#{uid} and deleted=0 and type =1
    </select>


</mapper>
